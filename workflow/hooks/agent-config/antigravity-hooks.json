{
  "deny-dangerous": {
    "enabled": true,
    "PreToolUse": [
      {
        "matcher": "run_command|view_file|write_to_file|replace_file_content|multi_replace_file_content",
        "hooks": [
          {
            "type": "command",
            "command": "gcd=\"$(git rev-parse --git-common-dir 2>/dev/null)\" || { printf '{\"decision\":\"deny\",\"reason\":\"Policy hook unavailable: git repository root unavailable.\"}\\n'; exit 0; }; case \"$gcd\" in */.git/modules/*|.git/modules/*) root=\"$(git rev-parse --show-toplevel 2>/dev/null)\" || { printf '{\"decision\":\"deny\",\"reason\":\"Policy hook unavailable: git repository root unavailable.\"}\\n'; exit 0; } ;; /*) root=\"$(dirname \"$gcd\")\" ;; *) root=\"$(git rev-parse --show-toplevel 2>/dev/null)\" || { printf '{\"decision\":\"deny\",\"reason\":\"Policy hook unavailable: git repository root unavailable.\"}\\n'; exit 0; } ;; esac; bash \"$root/.agents/hooks/deny-dangerous.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}

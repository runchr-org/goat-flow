/**
 * Public barrel for learning-loop migration helpers.
 * Keep the concrete migration implementations behind this small import surface.
 */
export { splitFootguns, mergeFootguns } from './split-footguns.js';
export { splitLessons, mergeLessons } from './split-lessons.js';

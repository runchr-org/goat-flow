{
  "skill": "goat-debug",
  "scenarios": [
    {
      "vagueInput": "the login is broken",
      "expectedQuestions": ["symptom", "area", "goal"],
      "idealSpecificInput": "Login returns 500 since commit abc123. Stack trace shows null session in AuthController:42."
    },
    {
      "vagueInput": "something's wrong with the API",
      "expectedQuestions": ["symptom", "error", "area"],
      "idealSpecificInput": "POST /api/users returns 422 with 'email required' even when email is provided in the request body."
    },
    {
      "vagueInput": "it crashed",
      "expectedQuestions": ["error", "what were you doing", "area"],
      "idealSpecificInput": "TypeError at src/auth/session.ts:42 — cannot read property 'id' of undefined when logging in with OAuth."
    }
  ]
}

---
category: index
bucket: footguns
last_reviewed: 2026-05-30
generated: false
---

# Footguns Index

Prototype index for active footgun entries. Each line links to the bucket file and includes a grep-friendly heading anchor.

- [Agent-produced output may contain control sequences that hijack the host terminal](agent-output-trust.md) (search: "## Footgun: Agent-produced output may contain control sequences that hijack the host terminal") - Raw TUI or ANSI output can corrupt host terminals, dashboard panes, or log viewers.
- [Provider message shapes have hard constraints that crash on naive slicing](agent-output-trust.md) (search: "## Footgun: Provider message shapes have hard constraints that crash on naive slicing") - Provider responses break when `None`, thinking blocks, or tool-result blocks are sliced like plain text.
- [Audit does not prove end-to-end deny enforcement at runtime](auditor.md) (search: "## Footgun: Audit does not prove end-to-end deny enforcement at runtime") - Local hook checks do not prove an external agent delivered the hook event.
- [Missing directories can false-pass when harness checks use `listDir()` as an existence test](auditor.md) (search: "## Footgun: Missing directories can false-pass when harness checks use `listDir()` as an existence test") - `listDir()` returns `[]` for missing paths, so checks need `exists()` when absence matters.
- [Structural Compliance Illusion](auditor.md) (search: "## Footgun: Structural Compliance Illusion") - Structural audit pass does not prove docs, ADRs, or prompts are semantically true.
- [TTL'd cache invalidation MUST travel with every writer, not just the writer the bug surfaced from](caching.md) (search: "## Footgun: TTL'd cache invalidation MUST travel with every writer, not just the writer the bug surfaced from") - Read-side caches can return plausible stale counts after writes unless every writer invalidates them.
- [Resource cleanup at one layer leaves the consumer loop running at the next layer](cleanup-layering.md) (search: "## Footgun: Resource cleanup at one layer leaves the consumer loop running at the next layer") - Timeouts can remove resources while consumers keep issuing fast-failing operations.
- [Host-native paths leak into user-visible CLI output on Windows](cli.md) (search: "## Footgun: Host-native paths leak into user-visible CLI output on Windows") - Backslash paths in Bash-facing prompts and output become invalid or misleading on Windows.
- [ESM main-module guard breaks under symlinks](cli.md) (search: "## Footgun: ESM main-module guard breaks under symlinks") - Symlinked CLI launches can exit with no output when `argv[1]` and `import.meta.url` differ.
- [Diagnostic logs to stdout corrupt structured-output modes](cli.md) (search: "## Footgun: Diagnostic logs to stdout corrupt structured-output modes") - Console output before JSON/SARIF/JSONL corrupts downstream parsers.
- [`value || DEFAULT` silently drops valid falsy values (0, "", false)](config.md) (search: "`value || DEFAULT` silently drops valid falsy values") - `||` erases explicit zero, empty string, or false overrides.
- [Misspelled config key silently disables the feature](config.md) (search: "## Footgun: Misspelled config key silently disables the feature") - Wrong config keys fall back to empty defaults and make features appear unconfigured.
- [`JSON.stringify` as a dedupe key silently drops function values, class instances, and BigInts](config.md) (search: "## Footgun: `JSON.stringify` as a dedupe key silently drops function values, class instances, and BigInts") - Dedupe by JSON string can collapse distinct config objects with non-JSON values.
- [Project-browser modal is reachable only via header-span click, not from the add-project flow](dashboard.md) (search: "## Footgun: Project-browser modal is reachable only via header-span click, not from the add-project flow") - Browse exists but is hidden from the add-project path users naturally try first.
- [Tailwind utility class names collide with custom component classes](dashboard.md) (search: "## Footgun: Tailwind utility class names collide with custom component classes") - Tailwind utilities can override custom CSS with equal or higher specificity.
- [Native Windows terminal sessions need both a Windows shell plan and a Windows runner shim](dashboard.md) (search: "## Footgun: Native Windows terminal sessions need both a Windows shell plan and a Windows runner shim") - Windows terminal launch needs both platform-specific shell spawning and runner-shim selection.
- [Dashboard reader decoders can erase score-critical API fields](dashboard.md) (search: "## Footgun: Dashboard reader decoders can erase score-critical API fields") - Browser decoders can drop discriminants and make UI scores disagree with API payloads.
- [Dashboard aggregate facts and Home agent cards can use different agent sets](dashboard.md) (search: "## Footgun: Dashboard aggregate facts and Home agent cards can use different agent sets") - Home cards and aggregate Agent Setup can disagree about which agents count.
- [Dashboard terminal helper tests can leak event-loop handles](dashboard.md) (search: "## Footgun: Dashboard terminal helper tests can leak event-loop handles") - VM-loaded helper tests can pass assertions while timers or sockets keep Node alive.
- [Dashboard terminal prompts can be dropped before browser attachment](dashboard.md) (search: "## Footgun: Dashboard terminal prompts can be dropped before browser attachment") - Launch prompts can disappear unless sent after browser terminal attachment and runner readiness.
- [Workspace terminal waiting state has multiple derived surfaces](dashboard.md) (search: "## Footgun: Workspace terminal waiting state has multiple derived surfaces") - Waiting state can diverge across header, pane, meters, and backend session lists.
- [Dashboard agent-targeting uses activeRunner where it should use the failing or selected agent](dashboard.md) (search: "## Footgun: Dashboard agent-targeting uses activeRunner where it should use the failing or selected agent") - Executor runner and prompt target agent are different facts.
- [Playbooks reference goat-flow repo-internal files absent from consumer installs](docs-and-crossrefs.md) (search: "## Footgun: Playbooks reference goat-flow repo-internal files absent from consumer installs") - Shipped playbooks can cite ADRs, CLI source, or learning-loop files that consumers do not have.
- [Flipping a doctrine in one playbook leaves siblings citing the old stance](docs-and-crossrefs.md) (search: "## Footgun: Flipping a doctrine in one playbook leaves siblings citing the old stance") - Related docs can structurally pass while contradicting the new doctrine.
- [Adding an instruction-file section ripples across four section-list sources plus the line target](docs-and-crossrefs.md) (search: "## Footgun: Adding an instruction-file section ripples across four section-list sources plus the line target") - Instruction sections are mirrored in parity scripts, setup guides, manifests, and line budgets.
- [Agent capability metadata goes stale when upstream docs add hooks](docs-and-crossrefs.md) (search: "## Footgun: Agent capability metadata goes stale when upstream docs add hooks") - Runtime support claims drift when upstream agents add new hook/config capabilities.
- [Hook additions and renames cross runtime, dashboard, and audit surfaces](docs-and-crossrefs.md) (search: "## Footgun: Hook additions and renames cross runtime, dashboard, and audit surfaces") - Hook changes must update registry, installer, manifest, preflight, configs, and docs together.
- [Active footgun Symptoms paragraph drifts after the underlying bug is fixed](docs-and-crossrefs.md) (search: "## Footgun: Active footgun Symptoms paragraph drifts after the underlying bug is fixed") - Active entries can keep stale symptoms even when prevention remains valid.
- [Adding a skill-playbook requires lock-step updates across 13+ surfaces](docs-and-crossrefs.md) (search: "## Footgun: Adding a skill-playbook requires lock-step updates across 13+ surfaces") - New playbooks require manifest, prompts, docs, drift, and installed-template parity updates.
- [Cross-reference fragility across docs](docs-and-crossrefs.md) (search: "## Footgun: Cross-reference fragility across docs") - Renamed or moved files can break dense pointer maps across multiple docs.
- [ADR renumbering breaks cross-references](docs-and-crossrefs.md) (search: "## Footgun: ADR renumbering breaks cross-references") - Numeric ADR references can still resolve while pointing to the wrong decision.
- [Version bump checks do not cover synthetic project config strings](docs-and-crossrefs.md) (search: "## Footgun: Version bump checks do not cover synthetic project config strings") - Version scripts can miss fixtures, helper scripts, and secondary reference strings.
- [Hot-path agent instructions drift unevenly across agents](docs-and-crossrefs.md) (search: "## Footgun: Hot-path agent instructions drift unevenly across agents") - One agent can receive weaker release or routing guidance than the others.
- [Filesystem-backed validation can miss untracked or ignored replacement files](docs-and-crossrefs.md) (search: "## Footgun: Filesystem-backed validation can miss untracked or ignored replacement files") - Local validation can pass with ignored replacement files that collaborators will not receive.
- [Prose examples for agent-specific paths drift from the manifest](docs-and-crossrefs.md) (search: "## Footgun: Prose examples for agent-specific paths drift from the manifest") - Agent-specific path examples can stop matching manifest truth.
- [Cold-path docs drift while structural audit passes](docs-drift.md) (search: "## Footgun: Cold-path docs drift while structural audit passes") - Audit can pass while cold-path docs contain false claims or dead paths.
- [Hook toggles can scaffold uninstalled agent surfaces](hooks.md) (search: "## Footgun: Hook toggles can scaffold uninstalled agent surfaces") - Hook toggles can create configs for agents the target project never installed.
- [Splitting a monolithic guardrail can drop parser coverage while preserving the headline checks](hooks.md) (search: "## Footgun: Splitting a monolithic guardrail can drop parser coverage while preserving the headline checks") - Small split hooks can pass smoke tests while losing wrapper, parser, and false-positive coverage.
- [Hook command strings can fail before guard code starts](hooks.md) (search: "## Footgun: Hook command strings can fail before guard code starts") - Configured launch strings can fail with 126/127 even when direct hook self-tests pass.
- [Hook launchers using --show-toplevel resolve to the worktree, not the main repo](hooks.md) (search: "## Footgun: Hook launchers using --show-toplevel resolve to the worktree, not the main repo") - Worktree sessions can resolve hook paths against the worktree instead of the main repo.
- [Extension-based secret checks can confuse filenames with query syntax](hooks.md) (search: "## Footgun: Extension-based secret checks can confuse filenames with query syntax") - Broad `.key`/`.pem` matching can block jq/yq fields or comment text.
- [File-read deny does not bind Bash shell reads of secret files](hooks.md) (search: "## Footgun: File-read deny does not bind Bash shell reads of secret files") - Read-tool deny patterns do not stop Bash commands such as `cat .env`.
- [GitHub CLI comments bypassed shared-system write guardrails](hooks.md) (search: "## Footgun: GitHub CLI comments bypassed shared-system write guardrails") - GitHub writes can happen through `gh` comments, API calls, and workflow commands without `git push`.
- [Copilot preToolUse hooks must distinguish structured payloads from Bash calls](hooks.md) (search: "## Footgun: Copilot preToolUse hooks must distinguish structured payloads from Bash calls") - Copilot non-Bash tool payloads must not be parsed as shell commands.
- [Installed settings.json deny patterns can silently drift from workflow templates](hooks.md) (search: "## Footgun: Installed settings.json deny patterns can silently drift from workflow templates") - Installed deny patterns can be weaker than templates unless parity checks cover them.
- [Codex workspace-root permission profiles must use the local 0.131 token](hooks.md) (search: "## Footgun: Codex workspace-root permission profiles must use the local 0.131 token") - Unsupported Codex permission tokens can be ignored or break startup.
- [Codex config preservation can leave old permission profiles behind](hooks.md) (search: "## Footgun: Codex config preservation can leave old permission profiles behind") - Upgrade preserves `.codex/config.toml`, so old permission profiles can remain after install.
- [Git push deny checks must normalize shell wrappers and control bodies](hooks.md) (search: "## Footgun: Git push deny checks must normalize shell wrappers and control bodies") - Push blocking must handle wrappers, assignments, conditionals, functions, and login shells.
- [Nested hook checks must reuse the command segment splitter](hooks.md) (search: "## Footgun: Nested hook checks must reuse the command segment splitter") - Nested `bash -c`, command substitution, and segment chains need the same splitter as top-level commands.
- [Heredoc masking can hide executable shell lines](hooks.md) (search: "## Footgun: Heredoc masking can hide executable shell lines") - Heredoc masking must mirror Bash delimiters or it can hide executable lines.
- [Internal / intermediate runs against a user target must strip side-effect-bearing config](internal-run-isolation.md) (search: "## Footgun: Internal / intermediate runs against a user target must strip side-effect-bearing config") - Meta-runs can pollute user output, logs, or traces if they reuse side-effect paths.
- [Quality reviews disappear when the agent skips the final JSON write](quality.md) (search: "## Footgun: Quality reviews disappear when the agent skips the final JSON write") - Quality history and diff are empty unless the agent actually writes the JSON report file.
- [Audit score tempering fields must survive every renderer](quality.md) (search: "## Footgun: Audit score tempering fields must survive every renderer") - Non-gating audit caveats must survive all renderers and prompt summaries.
- [Structural validation passes while content is still unanswerable](quality.md) (search: "## Footgun: Structural validation passes while content is still unanswerable") - Structurally valid artifacts can still contain unresolved questions or placeholders.
- [Audit checks must not prescribe machine-specific shared content](quality.md) (search: "## Footgun: Audit checks must not prescribe machine-specific shared content") - Checks must not push committed instructions toward machine-specific paths or values.
- [Advisory warnings without enforcement train users to ignore output](quality.md) (search: "## Footgun: Advisory warnings without enforcement train users to ignore output") - Permanent non-gating warning streams become noise unless they have a migration path.
- [YAML heredocs can break tooling before shell execution](quality.md) (search: "## Footgun: YAML heredocs can break tooling before shell execution") - Workflow heredocs must satisfy YAML tooling before shell execution starts.
- [Sentinel-position policy is invisible until the LM tries trailing output](sentinels.md) (search: "## Footgun: Sentinel-position policy is invisible until the LM tries trailing output") - Completion markers at the end of stdout fail when anything prints after them.
- [Common code-fence syntax collides with the agent's own work content](sentinels.md) (search: "## Footgun: Common code-fence syntax collides with the agent's own work content") - Generic action delimiters collide with real README or doc code fences.
- [Optional-hook agent profiles break when installer treats hooks as universal](setup.md) (search: "## Footgun: Optional-hook agent profiles break when installer treats hooks as universal") - Optional hook support must not be treated as universal install contract.
- [New-harness contributions can bypass the manifest-driven installer and modify shared core surfaces](setup.md) (search: "## Footgun: New-harness contributions can bypass the manifest-driven installer and modify shared core surfaces") - New harness support must use the manifest-driven contract instead of custom shared-surface forks.
- [Skill parity edits can miss `.github/skills/` and fail repo-level drift checks](skills.md) (search: "## Footgun: Skill parity edits can miss `.github/skills/` and fail repo-level drift checks") - Skill mirror edits must include GitHub Copilot skill surfaces.
- [Shared reference edits can split workflow templates from installed runtime copies](skills.md) (search: "## Footgun: Shared reference edits can split workflow templates from installed runtime copies") - Shared guidance changes must keep runtime copies and workflow templates aligned.
- [Skill reference-pack merges can leave stale installed files behind](skills.md) (search: "## Footgun: Skill reference-pack merges can leave stale installed files behind") - Upgrades can leave old per-skill reference files beside current SKILL.md files.
- [Bash-prescribed slash-command or skill bodies break under per-block tool isolation](skills.md) (search: "## Footgun: Bash-prescribed slash-command or skill bodies break under per-block tool isolation") - Multi-block Bash snippets in skill text lose shell state between tool invocations.
- [Release-version bumps can break skill-rename work through stale fixtures and hardcoded current-version routing](skills.md) (search: "## Footgun: Release-version bumps can break skill-rename work through stale fixtures and hardcoded current-version routing") - Release bumps can fail through stale fixtures and branch-routing constants.
- [New skill proposals can be configuration systems shaped around one workflow rather than general-purpose tools](skills.md) (search: "## Footgun: New skill proposals can be configuration systems shaped around one workflow rather than general-purpose tools") - Personal workflow preferences can masquerade as general-purpose canonical skills.
- [Linter or security-scanner output can pressure rewrites of load-bearing skill language](skills.md) (search: "## Footgun: Linter or security-scanner output can pressure rewrites of load-bearing skill language") - Prose rewrites can pass scanners while weakening behavior-shaping skill language.

# Commit Message

```
feat(m08): post-critique fixes from 11-project Codex review

Setup guard prevents rogue rewrites on current projects. compose-setup.ts returns early for v0.9/v1.0 upgrades. Removed CI checks, format-file.sh, guard-write-size.sh. SBAO and compliance mode now opt-in. Purged all stale v1.0 refs from skills. Added /api/rubrics endpoint, project-structure.json, split upgrade docs. Light mode fix.

1239 tests pass, scanner 100% (102 checks).
```
